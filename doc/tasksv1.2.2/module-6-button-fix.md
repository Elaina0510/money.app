# 模块 6：账单页 - 按钮位置修正

> 需求编号：#2
> 优先级：中
> 影响范围：前端 RecordDetailPage.vue

---

## 任务列表

### 6.1 前端 - 修正按钮布局

- [ ] 移除按钮的 `block` 属性
- [ ] 添加 `justify-center` 让按钮组居中
- [ ] 使用 `flex-grow-1` + `max-width: 200px` 让按钮等宽
- [ ] 添加 `mt-4` 与上方内容保持间距

### 6.2 前端 - 统一按钮风格

- [ ] 按钮使用 `rounded="xl"` 圆角
- [ ] 按钮使用 `variant="tonal"` 样式
- [ ] 按钮使用 `size="large"` 尺寸
- [ ] 与快速记账页的支出/收入按钮风格一致

---

## 验收标准

- [ ] 桌面端查看账单详情，编辑和删除按钮居中显示
- [ ] 移动端查看账单详情，按钮居中且不溢出屏幕
- [ ] 按钮风格与快速记账页一致（rounded-xl, tonal）
