# 模块 5：记账页 - 标签搜索联想

> 需求编号：#11
> 优先级：中
> 影响范围：前端 RecordFormPage.vue，后端 tags API
> 依赖：模块 2（需要标签带分类信息的 API）

---

## 任务列表

### 5.1 后端 - 标签搜索 API

- [ ] `GET /api/tags` 增加 `q` 查询参数
- [ ] `tag_service.get_tags` 支持 `search` 参数
- [ ] 搜索匹配 `Tag.name.contains(search)`
- [ ] 限制搜索结果数量为 20

### 5.2 前端 - 标签输入改为搜索联想

- [ ] 将 `v-combobox` 改为 `v-autocomplete`
- [ ] 设置 `no-filter` 禁用前端过滤
- [ ] 实现 `onTagSearch` 搜索逻辑（防抖 200ms）
- [ ] 点击输入框不显示历史记录

### 5.3 前端 - 选择标签自动填入分类

- [ ] 实现 `onTagSelected` 函数
- [ ] 选择标签后自动设置 `categoryId`
- [ ] 清空输入框时分类不受影响

### 5.4 前端 - 无匹配时创建新标签

- [ ] 显示"无匹配标签，按回车创建"提示
- [ ] 监听 `@keydown.enter` 事件
- [ ] 调用 `createTag` 创建新标签并选中

---

## 验收标准

- [ ] 点击标签输入框不显示历史记录列表
- [ ] 输入"午"后 200ms 内显示包含"午"的标签
- [ ] 选择"午餐"后标签填入，分类自动设为"餐饮"
- [ ] 输入无匹配文字时显示"无匹配标签"提示
- [ ] 按回车可创建新标签
