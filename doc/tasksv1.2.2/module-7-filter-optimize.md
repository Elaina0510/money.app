# 模块 7：账单页 - 分类筛选优化

> 需求编号：#3
> 优先级：中
> 影响范围：前端 RecordListPage.vue

---

## 任务列表

### 7.1 前端 - 移除搜索按钮

- [ ] 删除搜索按钮的 HTML（放大镜图标按钮）

### 7.2 前端 - 自动触发筛选

- [ ] 添加 `watch` 监听 `filters.type` 和 `filters.category_id` 变化
- [ ] 实现 `debouncedSearch` 防抖函数（300ms）
- [ ] 筛选条件变化时自动调用 `search()`

### 7.3 边界处理

- [ ] 日期筛选由月份选择器触发，已有 `selectMonth` 函数处理
- [ ] 快速切换筛选条件时防抖生效，无重复请求

---

## 验收标准

- [ ] 选择分类后立即显示筛选结果（300ms 内）
- [ ] 切换类型筛选后立即更新结果
- [ ] 搜索按钮已移除
- [ ] 快速切换筛选条件时无重复请求
